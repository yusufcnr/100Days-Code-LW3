const myDog = {
  // Only change code below this line

"name":"nameDog",
"legs":4,
"tails":1,
"friends": ["emine", "ali", "zeyneğp"],

  // Only change code above this line
};

console.log(typeof(myDog["friends"]));